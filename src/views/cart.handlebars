<h1>Carrito</h1>

<table>
    <tr>
        <th>Nombre</th>
        <th>Cantidad</th>
    </tr>
    {{#each cart.products}}
    <tr>
        <td>{{this.product.title}}</td>
        <td>{{this.quantity}}</td>
    </tr>
    {{/each}}
</table>
